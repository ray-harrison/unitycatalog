# Azure AD Authentication Configuration
# Copy this file to .env.local and fill in your Azure AD application details
# IMPORTANT: .env.local is gitignored and should contain your actual credentials

# Enable/disable Azure AD authentication
# Set to "true" to enable Azure AD, "false" to disable
REACT_APP_MS_AUTH_ENABLED=false

# Azure AD Single Page Application (SPA) Client ID
# Get this from Azure Portal > App registrations > Your SPA app > Overview > Application (client) ID
REACT_APP_MS_CLIENT_ID=your-spa-client-id-here

# Azure AD Authority URL (tenant-specific)
# Format: https://login.microsoftonline.com/{tenant-id}
# Replace {tenant-id} with your Azure AD tenant ID
REACT_APP_MS_AUTHORITY=https://login.microsoftonline.com/your-tenant-id-here

# Optional: Redirect URI after authentication
# Defaults to window.location.origin if not specified
# For local development: http://localhost:3000
# For production: https://your-domain.com
REACT_APP_MS_REDIRECT_URI=http://localhost:3000

# Optional: Post-logout redirect URI
# Where to redirect after user logs out
# Defaults to window.location.origin if not specified
REACT_APP_MS_POST_LOGOUT_REDIRECT_URI=http://localhost:3000

# Optional: OAuth scopes
# Comma-separated list of scopes to request
# Default: openid,profile,email
REACT_APP_MS_SCOPES=openid,profile,email,User.Read
